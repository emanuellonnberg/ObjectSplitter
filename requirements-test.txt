# Test dependencies for ObjectSplitter
# Install with: pip install -r requirements-test.txt
trimesh>=3.20.0
numpy>=1.20.0
scipy>=1.7.0
pytest>=7.0.0

# Required by trimesh for polygon area and path operations
shapely>=2.0.0
networkx>=3.0.0

# Optional - for PNG image rendering in tests
matplotlib>=3.5.0
